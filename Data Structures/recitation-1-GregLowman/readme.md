# Employee Management — C++

A C++ command-line application for managing employee records loaded from a CSV file.

## Overview

Reads employee data from `employees.csv` and supports basic record operations via `addEmployee`.

## Build

```bash
g++ -o main main.cpp addEmployee.cpp
```

## Usage

```bash
./main
```
