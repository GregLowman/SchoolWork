# Functions

A minimal C++ project demonstrating a basic function implementation with a CMake and Google Test build pipeline.

## Overview

Implements a single integer-returning function (`fooA`) used to verify the build and test setup.

## Build

```bash
mkdir build && cd build
cmake ..
make
```

## Usage

```bash
./run_app_1
./run_tests_1
```
